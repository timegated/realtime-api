### A Simple Real Time API

- Socket.IO and Express.
- A simple interface built with React
- **Should** be able to handle concurrent requests to a db and notify
all parties currently connected that a change has occured.


